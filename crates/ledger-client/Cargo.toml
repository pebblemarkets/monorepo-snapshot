[package]
name = "pebble-ledger-client"
version = "0.1.0"
edition = "2021"

[dependencies]
pebble-daml-grpc = { path = "../daml-grpc" }
thiserror = "2.0.18"
tokio = { version = "1.49.0", features = ["time"] }
tonic = "0.12"
uuid = { version = "1", features = ["v4"] }
