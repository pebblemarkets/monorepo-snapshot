[package]
name = "pebble-daml-grpc"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
prost-types = "0.13"
tonic = { version = "0.12", features = ["tls", "tls-roots"] }

[build-dependencies]
protoc-bin-vendored = "3"
tonic-build = "0.12"

